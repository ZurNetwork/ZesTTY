declare const n: number;
declare function expensive(x: number): number;

const result = if (n > 0) {
  const cached = expensive(n);
  cached * 2
} else {
  0
};

const chained: string = if (n > 100) {
  const s = `${n}`;
  s + "!"
} else if (n > 10) {
  "mid"
} else {
  const neg = -n;
  `${neg}`
};

console.log(result, chained);
