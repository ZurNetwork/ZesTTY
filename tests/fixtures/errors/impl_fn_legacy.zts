interface Display<Self> {
  fmt(self: Self): string;
}

enum Shape {
  Circle { r: number },
}

// v1 (0.3.x) member syntax: dedicated migration diagnostic in 0.4.0.
impl Display for Shape {
  fn fmt(self): string {
    return "";
  }
}
