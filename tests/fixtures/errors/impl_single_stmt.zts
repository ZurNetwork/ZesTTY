enum Shape {
  Circle { r: number },
}

// An impl in a single-statement slot has no statement list to pair with
// its enum; lowering could never merge it.
if (true) impl Display for Shape {
  fmt(self): string {
    return "";
  }
}
